# 21 — План поставки и бэклог

**Статус:** 🟢 v1.0 план, 22 августа 2026. **Владелец документа:** ведущий разработчик. **Утверждает:** Alex Bayov.

Этот документ — единственный источник **порядка работ**. Что именно делать в каждом тикете — в [`22-developer-work-packages.md`](22-developer-work-packages.md). Контракты данных — в [`23-data-contracts-and-content-pipeline.md`](23-data-contracts-and-content-pipeline.md). Проверки и гейты — в [`24-test-matrix-and-release-gates.md`](24-test-matrix-and-release-gates.md).

---

## 1. Executive status (факт на 22 августа 2026)

| Параметр | Фактическое значение | Как проверено |
|---|---|---|
| Ветка / базовый коммит | `main`, отслеживает `origin/main`, HEAD = `3f87b80 docs: add delivery plan and developer work packages` | `git status`, `git log` |
| Состояние M3-D | **закоммичен и отправлен в `origin`** коммитом `61c991b feat(M3-D): harden runtime and release readiness`; `W0-01` закрыт **частично** — критерии чистого дерева, ветки/PR/зелёного CI и разделения на 4 коммита не выполнены (doc 22 §`W0-01`) | `git log --oneline`, `git branch -avv`, `git ls-remote --heads origin`, `gh pr list`, `gh run list` |
| Незакоммиченные изменения | тикет `W0-04` (`code/package.json` — скрипт `verify` и `engines.node`, `code/.nvmrc`; корневой `.github/workflows/quality.yml` переведён на `npm run verify` и `node-version-file`; удалён инертный `code/.github/`) и текущий проход `W0-02`/`W0-03`/`W0-05` | `git status --porcelain` |
| Автотесты | **17 файлов / 128 тестов, все проходят локально** | `npm test` |
| Игровой контент | 1 зона «Ближняя окраина», 3 encounter (`perimeter-checkpoint`, `collapsed-yard`, `relay-station`) | `public/config/*.json` |
| Каталог экипировки | 4 оружия, 3 брони, 3 патрона, 3 архетипа врага | `public/config/equipment.json` |
| Сохранение | schema v4, ключ `eden.save.v4`, миграция v3→v4, строгая cross-field валидация | `code/src/game/save.ts` |
| Бюджет бандла | initial JS 36.2 kB gzip (лимит 150), combat lazy 349.7 kB gzip (порог 1200) — фактический вывод на 22 авг 2026 | `npm run analyze:budget` |
| Стек | Phaser 4.2.1 (lazy), Preact 10, TypeScript ~6.0, Vite 8, Vitest 4; CI Node 22 | `package.json`, `.github/workflows/quality.yml` |

### Чего фактически НЕТ

- **Никакой браузерной и устройственной проверки не проводилось.** Нет headless-браузера, нет jsdom, нет E2E-инструмента в `devDependencies`. Все 128 тестов — статические и модельные (чистые функции, фикстуры, чтение CSS/TSX как текста).
- Не измерена реальная производительность в браузере: бюджет бандла — статический gzip-гейт, не runtime-профиль.
- Не проверена геометрия рендера ни на одном viewport, включая 390×844 и 360×640.
- Нет прогрессии уровней/навыков/перков, нет 4 зон, нет 6 типов врагов, нет финальной миссии, нет туториала.
- Нет штрафа смерти: поражение не отнимает ни XP, ни ресурсы, ни предметы (решение D-01, реализация `W4-02`/`W5-05`).
- Нет реального objective runtime: единственное условие победы — «все враги мертвы»; `secure` только подпись цели.
- Нет ассетов (арт, иконки, звук), нет аудио-слоя и нет платформенных интеграций.
- Каталоги `design-data/` и `art/` созданы тикетом `W0-03`, но **пусты**: ни одного отчёта симулятора, perf или ручной QA пока нет.

---

## 2. Граница scope плана

Этот план ведёт проект от текущего M3-D alpha к **release candidate полного MVP** по границам [`20-mvp-scope-and-roadmap.md`](20-mvp-scope-and-roadmap.md): 4 зоны, 6 типов врагов, 7 оружий, 8 ресурсов, 3 уровня базы, 10–15 часов основного контента, desktop + mobile, без P2W.

**Внутри плана:** инженерная реализация, QA-инфраструктура, баланс, интеграция контента и ассетов, подготовка релиза.

**Вне плана:** производство арта и звука (внешняя работа, см. §10), финальные тексты нарратива, платформенные юридические/консент-требования, серверная часть (cloud save, authoritative leaderboards, anti-tamper).

**Допущение по ресурсам:** один разработчик full-time, владелец продукта — part-time для решений. План **последовательный**: параллельные треки не запланированы, потому что почти каждый пакет зависит от QA-инфраструктуры и решений по балансу.

---

## 3. Приоритеты

### P0 — блокируют всё остальное

| ID | Задача | Почему P0 | Пакет |
|---|---|---|---|
| P0-A | ✅ Закоммитить и отправить M3-D | закрыто: `61c991b` в `origin`, работа больше не существует в единственной копии | W0-01 |
| P0-B | Браузерный E2E/DOM harness | без него ни одно UX-утверждение нельзя подтвердить; блокирует W2, W8, W10 | W1-01…W1-04 |
| P0-C | Ручная mobile QA по скрипту | 44px/geometry/touch не проверены ни разу | W1-05, W2-06 |
| P0-D | Симулятор баланса | без него прогрессия и экономика будут выдумываться, а не считаться | W3-01…W3-03 |
| P0-E | Чистка документации от непроверенных утверждений | документы расходились с репозиторием (отсутствовавшие `design-data/` и `art/`, устаревший статус M3-D, заявленный scope против фактического) | W0-03, W0-05 |
| P0-F | Решение по штрафу смерти (P0-1 из аудита) | блокирует W4 и W5 одновременно | решение D-01 |

### P1 — нужны до content lock

| ID | Задача | Пакет |
|---|---|---|
| P1-A | Прогрессия L1–L6: кривая XP, штраф смерти, save v5 | W4-01, W4-02, W4-05 |
| P1-B | База/инвентарь/крафт до MVP-объёма | W5 |
| P1-C | Objective runtime (реальные `secure`/`retrieve`/`escape`) | W6-01 |
| P1-D | Roster 6 врагов и AI-поведения | W6-02, W6-06 |
| P1-E | Структура 4 зон и pipeline авторинга карт | W7-01, W7-02 |
| P1-F | Design tokens и asset registry с fallback | W8-01, W8-02 |
| P1-G | Экран настроек с сохранением состояния | W9-03 |

### P2 — после content lock или после решений владельца

| ID | Задача | Пакет |
|---|---|---|
| P2-A | Навыки/SPECIAL/перки | W4-03, W4-04 |
| P2-B | Финальный арт и боевые эффекты | W8-03…W8-05 |
| P2-C | Аудио-слой | W9-01, W9-02 |
| P2-D | Платформенные интеграции и cloud save | W10-01…W10-03 |
| P2-E | Косметика и добровольная реклама | вне плана до platform review |

---

## 4. Workstreams

| ID | Workstream | Ответственность | Пакеты |
|---|---|---|---|
| WS-ENG | Runtime и игровые системы | ведущий разработчик | W4, W5, W6 |
| WS-QA | Тестовая инфраструктура и ручная QA | ведущий разработчик | W1, часть W0 |
| WS-BAL | Симуляция, баланс, экономика | разработчик + владелец продукта | W3 |
| WS-CONTENT | Каталоги контента, зоны, авторинг | разработчик + владелец продукта | W7, часть W5 |
| WS-UX | Ввод, доступность, mobile, UI-интеграция | разработчик | W2, W8, W9-03 |
| WS-EXT | Арт и аудио (внешняя поставка) | владелец продукта / подрядчик | вход для W8, W9 |
| WS-PLAT | Платформа, сборка, релиз | разработчик + владелец продукта | W0, W10 |

---

## 5. Восьминедельный последовательный план

Каждая неделя заканчивается зелёным `npm run lint && npm run typecheck && npm test && npm run build && npm run analyze:budget` и обновлением статусов в [`00-index.md`](00-index.md).

### Неделя 1 — Стабилизация. Никаких новых механик

Единственная неделя, в которой запрещено добавлять игровые механики. Цель — превратить непроверяемое состояние в проверяемое.

| Порядок | Работа | Тикеты | Выход |
|---:|---|---|---|
| 1 | ✅ Закоммитить и отправить M3-D | W0-01 | сделано: `61c991b` в `origin` |
| 2 | Гигиена репозитория и CI | W0-02, W0-03, W0-04 | `npm run verify`, честная структура репозитория; `W0-02`/`W0-03` выполнены, `W0-04` не закоммичен |
| 3 | Поднять браузерный E2E harness и smoke-тест загрузки | W1-01 | первый реальный запуск приложения в браузере под тестом |
| 4 | DOM-тесты shell + happy path E2E | W1-02, W1-03 | подтверждённый путь base → миссия → награда → base |
| 5 | E2E save/reload/recovery | W1-04 | подтверждённое поведение сохранения в реальном localStorage |
| 6 | Проверки viewport 360×640 / 390×844 / 1280×720 | W1-05 | первое фактическое утверждение о геометрии |
| 7 | Ручной mobile QA по скрипту, дефекты в бэклог | W2-06 | заполненный чек-лист устройств, список дефектов |
| 8 | Каркас симулятора баланса и первый прогон | W3-01 | детерминированный отчёт по трём encounter |
| 9 | Чистка документации: убрать всё непроверенное | W0-05 | выполнено: docs 00, 09–20 согласованы с репозиторием |

**Гейт недели 1 (G0 + G1):** M3-D в remote ✅; E2E harness в CI и зелёный — **не сделано**; чек-лист mobile QA заполнен фактическими результатами — **не сделано**; отчёт симулятора существует — **не сделано**; ни один документ не утверждает непроверенное ✅ (`W0-05`).

### Неделя 2 — Ввод, доступность, mobile

| Работа | Тикеты | Выход |
|---|---|---|
| Единый action layer для клавиатуры/мыши/тача | W2-01 | один путь принятия ввода, гейтинг из `input-gating.ts` расширен |
| Long-press radial menu для 6 частей тела | W2-02 | бой играется одной рукой без hover |
| Фокус-порядок, видимый фокус, полная клавиатурная навигация | W2-03 | ни один экран не требует мыши |
| `aria-live` для боевого лога, роли и подписи | W2-04 | скринридер озвучивает результат выстрела |
| Safe-area, landscape, webview viewport | W2-05 | нет перекрытия CTA на 360×640 и 390×844 |
| Дефекты из недели 1 | из бэклога | закрыты или явно отложены с обоснованием |

**Гейт недели 2 (G2):** WCAG-проверки из doc 24 §7 проходят на E2E; ручной mobile-скрипт проходит без дефектов severity S1/S2.

### Неделя 3 — Баланс и экономика

| Работа | Тикеты | Выход |
|---|---|---|
| Метрики симулятора: TTK, win rate, полученный урон, расход патронов/прочности | W3-02 | воспроизводимый отчёт по seed |
| Экономическая симуляция: ресурсы, крафт, ремонт, лечение | W3-03 | прогноз ресурсного потока на MVP |
| Предложение balance lock v1 и обновление doc 12 | W3-04 | числа обоснованы прогонами, а не мнением |
| Границы баланса как регрессионные тесты | W3-05 | нарушение баланса ломает CI |
| Кривая XP L1–L6 | W4-01 | таблица уровней с обоснованием |
| Единая формула штрафа смерти | W4-02 | противоречие P0-1 закрыто |

**Гейт недели 3 (G3):** balance lock v1 утверждён владельцем; docs 05/12 не противоречат друг другу и коду; границы закреплены тестами.

### Неделя 4 — Прогрессия и save v5

| Работа | Тикеты | Выход |
|---|---|---|
| Save schema v5 + миграция v4→v5 | W4-05 | прогрессия персистится, старые сохранения читаются |
| Прогрессия в runtime и UI | W4-01 (доводка) | уровень, XP и штраф видны игроку |
| Навыки/SPECIAL/перки — только если решение D-02 принято | W4-03, W4-04 | иначе пакет переносится в post-MVP |

**Гейт недели 4:** миграция v4→v5 покрыта тестами в обе стороны отказа; E2E подтверждает сохранение прогрессии после reload.

### Неделя 5 — База, инвентарь, крафт

| Работа | Тикеты | Выход |
|---|---|---|
| 3 узла × 3 уровня, каталог улучшений | W5-01 | база достигает MVP-объёма |
| Крафт v1 (6–8 рецептов, без real-time таймеров) | W5-02 | атомарные транзакции, нет частичных списаний |
| Инвентарь: вес, стаки, quick slots в бою | W5-03 | расходники применяются в бою |
| Ремонт и разборка с понятным UI | W5-04 | игрок видит стоимость и результат до действия |
| Штраф смерти по лут-правилу из D-01 | W5-05 | смерть имеет цену без эскалации |

**Гейт недели 5:** каждая транзакция инвентаря/базы покрыта детерминированным тестом; E2E проходит цикл вылазка → лут → крафт → ремонт → следующая вылазка.

### Неделя 6 — Бой и AI

| Работа | Тикеты | Выход |
|---|---|---|
| Реальный objective runtime | W6-01 | `secure` перестаёт быть переименованным `eliminate` |
| AI: выбор цели, использование укрытий, пороги отступления | W6-02 | поведения читаемы и детерминированы по seed |
| Полный цикл статусов и поз + breakdown в UI | W6-03 | игрок понимает, почему промах |
| Overwatch v2 | W6-04 | реакция и её стоимость явные |
| Экономика патронов/прочности/осечек в бою | W6-05 | ресурсы боя связаны с базой |
| Roster 6 врагов | W6-06 | контент боя на MVP |

**Гейт недели 6:** симулятор подтверждает целевые TTK/win rate для всех 6 архетипов; ни один бой не завершается недетерминированно при фиксированном seed.

### Неделя 7 — Зоны, нарратив-каркас, UI

| Работа | Тикеты | Выход |
|---|---|---|
| Структура 4 зон и pipeline авторинга карт | W7-01, W7-02 | контент добавляется данными, без кода |
| Каркас нарратива (движок реплик, без финальных текстов) | W7-03 | тексты подключаются как контент |
| Туториал/онбординг первых 10 минут | W7-05 | цель понятна без внешней документации |
| Design tokens + asset registry с fallback | W8-01, W8-02 | арт подключается без переписывания UI |
| Экран настроек: звук, масштаб, контраст, reduced motion | W9-03, W9-04 | настройки сохраняются локально |

**Гейт недели 7 (G4):** структурный content lock — все 4 зоны существуют как валидные данные; UI работает на placeholder-ассетах; отсутствие финального арта/звука не ломает игру.

### Неделя 8 — Платформа, релиз, hardening

| Работа | Тикеты | Выход |
|---|---|---|
| Спецификация платформенной совместимости | W10-01 | решение D-05 обосновано фактами |
| SDK-абстракция с no-op реализацией по умолчанию | W10-02 | игра работает без платформы |
| Дизайн cloud save / anti-tamper (без реализации) | W10-03 | понятна цена авторитетных лидербордов |
| Сборка, деплой, релизный чек-лист | W10-04 | воспроизводимый релизный артефакт |
| Hardening: производительность, дефекты, финальный проход | W10-05 | нет открытых S1/S2 |

**Гейт недели 8 (G5):** release candidate; все гейты G0–G4 закрыты; полный прогон матрицы тестов из doc 24; финал проходится без покупок и рекламы.

---

## 6. Зависимости

```
W0-01 ✅ ─► всё остальное (выполнено: M3-D в `origin`, работа поверх него разблокирована)
W0-04 ─► W1-07 (единая verify-команда до CI-интеграции E2E)
W1-01 ─► W1-02 ─► W1-03 ─► W1-04 ─► W1-05 ─► W2-06
W1-05 ─► W2-02, W2-05, W8-03   (геометрию нельзя править вслепую)
W1-06 ─► W10-05                (hardening без профиля бессмыслен)
W3-01 ─► W3-02 ─► W3-03 ─► W3-04 ─► W3-05
W3-04 ─► W4-01, W5-02, W6-06   (контент не тюнится до balance lock)
D-01  ─► W4-02 ─► W4-05 ─► W5-05
D-02  ─► W4-03, W4-04
W4-05 ─► W5-01…W5-05           (схема сохранения раньше новых сущностей)
W6-01 ─► W7-02                 (карты авторятся под реальные objective)
W8-01 ─► W8-02 ─► W8-03…W8-05
WS-EXT (арт/звук) ─► W8-03, W8-04, W8-05, W9-01, W9-02
D-05  ─► W10-01 ─► W10-02 ─► W10-03
```

Жёсткие правила порядка:

1. Ни один тикет не начинается до `W0-01` — условие выполнено (`61c991b`).
2. Ни один UX-тикет с утверждением о геометрии или тач-вводе не закрывается до `W1-05`.
3. Ни один тюнинг чисел не закрывается до `W3-04`.
4. Ни одна новая сущность в сохранении не добавляется до `W4-05`.
5. Пакеты, зависящие от WS-EXT, не стартуют до поступления ассетов и спецификаций (см. §10).

---

## 7. Реестр рисков

| ID | Риск | Вероятность | Влияние | Триггер | Митигация | Владелец |
|---|---|---|---|---|---|---|
| R-01 | ✅ Закрыт: потеря незакоммиченного M3-D | — | — | — | `W0-01` выполнен, `61c991b` в `origin` | разработчик |
| R-02 | Первый реальный запуск в браузере выявит блокирующие дефекты рендера/геометрии | **высокая** | высокое | `W1-03`/`W1-05` красные | буфер 2 дня в неделе 1; дефекты приоритезируются выше новых механик | разработчик |
| R-03 | Phaser 4 несовместим с целевыми мобильными webview | средняя | высокое | сбои в `W1-05`/`W1-06` | ранняя проверка на устройстве; открытое решение Phaser 3/4 (D-04) держать открытым до недели 2 | разработчик |
| R-04 | Отсутствие ассетов блокирует W8/W9 в неделях 7–8 | **высокая** | среднее | нет поставки к неделе 6 | placeholder-контракт и fallback в `W8-02`; игра обязана быть проходимой на placeholder | владелец продукта |
| R-05 | Решения D-01…D-06 не приняты вовремя | средняя | высокое | конец недели 2 без решений | явные дедлайны решений в §9; при просрочке действует консервативный вариант из doc 20 | владелец продукта |
| R-06 | Баланс 10–15 часов не сходится с 4 зонами | средняя | высокое | отчёт `W3-03` | сокращать длительность, а не добавлять гринд; фиксировать в doc 12 | владелец продукта |
| R-07 | Рост бандла из-за арта/аудио выбивает бюджет | средняя | среднее | `analyze:budget` красный | бюджеты по типу ассетов в doc 24 §6; ленивые чанки на зону | разработчик |
| R-08 | Платформенные API окажутся несовместимы с no-P2W моделью | низкая | среднее | результат `W10-01` | SDK-абстракция с no-op по умолчанию (`W10-02`) | разработчик |
| R-09 | Save v5 миграция ломает существующие сохранения игроков | низкая | высокое | ошибки в `W4-05` | обязательные тесты миграции в обе стороны + backup-ключ | разработчик |
| R-10 | Расползание scope в W6 (стелс, мораль, очередь) | средняя | среднее | тикеты вне списка | scope boundary из doc 20; всё лишнее — post-MVP | разработчик |
| R-11 | Один разработчик = единая точка отказа плана | средняя | высокое | отсутствие > 3 дней | документированные контракты (docs 22–24) вместо знания в голове | владелец продукта |

---

## 8. Definition of Ready / Definition of Done

### Definition of Ready (тикет можно брать)

1. Есть цель одним предложением и явный источник (документ или файл кода).
2. Перечислено, что **вне** scope тикета.
3. Контракт данных/API описан или явно указано, что не меняется.
4. Критерии приёмки проверяемы автоматически или ручным скриптом.
5. Все зависимости закрыты, все нужные решения владельца приняты.
6. Указана оценка S/M/L.
7. Если тикет требует ассетов или платформенной спецификации — они уже поступили.

### Definition of Done (тикет закрыт)

1. `npm run lint`, `npm run typecheck`, `npm test`, `npm run build`, `npm run analyze:budget` зелёные.
2. Новое поведение покрыто тестом, который падает без изменения.
3. Если тикет затрагивает UI/ввод — есть E2E или DOM-проверка, а не только статическая.
4. Если тикет затрагивает числа — есть прогон симулятора или граничный тест.
5. Обновлены затронутые документы 09–24 и статус в `00-index.md`.
6. Изменение закоммичено в ветке и отправлено в PR; PR описывает что проверено и **что не проверено**.
7. В документах нет утверждений о браузерной/устройственной проверке, которой не было.
8. Временные файлы и отладочный код удалены.

---

## 9. Релизные гейты

| Гейт | Название | Условие прохождения | Ожидаемая неделя |
|---|---|---|---|
| **G0** | Baseline committed | M3-D в `origin`, CI зелёный на удалённой ветке | 1 |
| **G1** | Verifiable | E2E/DOM harness в CI; happy path, save/reload и viewport-проверки зелёные | 1 |
| **G2** | Mobile & a11y | ручной mobile-скрипт пройден; WCAG-проверки doc 24 §7 зелёные; нет S1/S2 | 2 |
| **G3** | Balance lock v1 | числа обоснованы симулятором, утверждены владельцем, закреплены тестами | 3 |
| **G4** | Content lock | 4 зоны как валидные данные, objective runtime реальный, игра проходима на placeholder-ассетах | 7 |
| **G5** | Release candidate | полная матрица doc 24; профиль производительности в пределах бюджета; финал без покупок и рекламы; нет S1/S2 | 8 |

Гейт нельзя закрыть частично. Если условие не выполнено — работа следующей недели не начинается, план сдвигается, и это фиксируется в changelog `00-index.md`.

### Решения, требуемые от владельца продукта

| ID | Решение | Блокирует | Дедлайн |
|---|---|---|---|
| D-01 | Единая формула штрафа смерти (кандидат 05.3.4 против консервативного варианта doc 20) | W4-02, W4-05, W5-05 | конец недели 2 |
| D-02 | Входят ли навыки/SPECIAL/перки в MVP или уходят в post-MVP | W4-03, W4-04 | конец недели 3 |
| D-03 | Целевая длительность: 10–15 часов против сокращённого честного MVP | W3-03, W3-04, W7-01 | конец недели 3 |
| D-04 | Phaser 4 остаётся или миграция на Phaser 3 | W1-06, W10-05 | конец недели 2 (после первых замеров) |
| D-05 | Нужны ли платформенные интеграции в первом релизе (реклама/лидерборды/cloud save) | W10-01…W10-03 | конец недели 6 |
| D-06 | Источник и бюджет арта и аудио, срок поставки | W8-03…W8-05, W9-01, W9-02 | конец недели 4 |

---

## 10. Что нельзя реализовать сейчас

Следующие пакеты **не могут быть начаты** и не входят в оценку сроков до появления внешних входов:

- **W8-03…W8-05 (арт-интеграция).** Нет ни одного production-ассета. После чистки `W0-02` в репозитории осталась единственная графика — `code/public/favicon.svg`; неиспользуемые starter-файлы (`src/assets/hero.png`, `preact.svg`, `vite.svg`, `public/icons.svg`) удалены как мёртвые. Каталог `art/` создан в `W0-03` и пуст; реестр `art/registry.json` вводится в `W8-02`.
- **W9-01, W9-02 (аудио).** Нет ни одного звукового файла и нет лицензионного реестра.
- **W10-01…W10-03 (платформа).** Нет локальной спецификации платформенных API; ни одна интеграция не подтверждена.

> **Явное ограничение.** [`13-narrative-and-setting.md`](13-narrative-and-setting.md), [`15-audio-design.md`](15-audio-design.md) и [`19-art-and-design-brief.md`](19-art-and-design-brief.md) — это **направления и требования, а не реализуемые спецификации**. Прежде чем стартуют W7-03/W7-04 (нарративный контент), W9-01/W9-02 (аудио) и W8-03…W8-05 (арт), для каждого из этих трёх документов нужен отдельный implementation spec с конкретным перечнем ассетов/текстов, их id, форматами, размерами, лицензиями, автором и fallback-поведением. До этого соответствующие тикеты остаются в состоянии **Blocked (external input)** и не считаются частью восьминедельного объёма.

---

## 11. Changelog

| Дата | Изменение |
|---|---|
| 22 авг 2026 | v1.0: зафиксирован фактический статус M3-D, 8-недельный последовательный план, приоритеты P0/P1/P2, workstreams, зависимости, реестр рисков, DoR/DoD, гейты G0–G5, решения D-01…D-06 и перечень внешне заблокированных пакетов. |
| 22 авг 2026 | `W0-01`/`W0-02`/`W0-03`/`W0-05`: executive status приведён к факту — M3-D закоммичен и отправлен (`61c991b`), HEAD = `3f87b80`, незакоммиченным остаётся `W0-04`. P0-A и риск R-01 закрыты. Уточнён перечень отсутствующего (штраф смерти, objective runtime, аудио-слой). Ссылки на удалённые starter-ассеты заменены фактическим состоянием после чистки `W0-02`. Каталоги `design-data/` и `art/` созданы и описаны. |
