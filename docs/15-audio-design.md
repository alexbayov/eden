# 15 — Audio Design

**Статус:** 🔴 не реализовано; заблокировано. Нужен implementation spec **и** ассеты.

> **Фактическое состояние:** в `code/` **нет аудио-слоя** — ни движка звука, ни микшера, ни вызовов воспроизведения, ни зависимости для звука в `package.json`. В репозитории **нет ни одного звукового файла**. Матрица ниже — целевое направление, а не описание игры.
>
> **Блокировка.** Пакеты `W9-01`/`W9-02` (аудио) не могут стартовать до появления отдельного implementation spec (конкретные id, форматы, длительности, громкости, лицензии, fallback) и фактической поставки файлов с локально проверенными лицензиями. Источник и бюджет аудио — решение D-06. Реестр аудио — формат `kind: "audio"` из [`23-data-contracts-and-content-pipeline.md`](23-data-contracts-and-content-pipeline.md) §7.

## MVP audio matrix (целевая)

| Слой | Контент | Правило |
|---|---|---|
| UI | click, confirm, error | короткий, различимый |
| Бой | выстрелы по классу, попадание, промах, броня, осечка | сигнал результата без громкости-спама |
| Статус | stun, blind, immobilize, OW | отдельный узнаваемый cue |
| Мир | ветер, металл, дальний сигнал | loop ≤60 с, низкая интенсивность |
| Музыка | база, исследование, бой, финал | crossfade, mute доступен |

Приоритет: gameplay feedback > ambience > music. Первый playable может использовать временные лицензированные/синтезированные локальные ассеты, но список лицензий обязателен.

## Acceptance criteria (целевые; ни один не выполнен)

- Все боевые состояния имеют звуковой и визуальный/текстовый feedback. — Визуальный/текстовый feedback боевых состояний в интерфейсе есть; **звукового нет**.
- Музыку и SFX можно раздельно выключить; состояние сохраняется. — Экрана настроек в коде нет (вводится `W9-03`), выключать нечего.
- Нет автозапуска звука до пользовательского взаимодействия. — Формально выполняется, потому что звука нет вообще; как гарантия не проверено.
- Mobile-микс не клиппует и не мешает речи/тексту. — Проверить невозможно: нет ни микса, ни устройственной QA.

## Открытые решения

- Музыкальный язык и нужен ли голос отца.
- Финальный формат исходников и бюджет записей.

## Post-MVP

Динамический stem-mix, фракционные музыкальные темы и полноценные аудиодневники.
