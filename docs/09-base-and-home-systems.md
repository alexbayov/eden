# 09 — Base & Home Systems

**Статус:** 🟡 реализован M2 vertical slice (17 авг. 2026). Это **не** база полного MVP.

**Реализованный слайс против целевого MVP.** Ниже зафиксировано и то, что есть в `code/`, и то, чего нет. Целевой MVP по [`20-mvp-scope-and-roadmap.md`](20-mvp-scope-and-roadmap.md) — 3 уровня базы и полный каталог узлов/рецептов; фактически реализован hub с **одним** рецептом, **одним** улучшением склада, medbay и ремонтом. Разрыв закрывается пакетом W5.

| Параметр | Целевой MVP | Реализовано в `code/` |
|---|---|---|
| Узлы базы | полный набор, 3 уровня | stash / workbench / medbay в модели; одно data-driven улучшение склада |
| Рецепты | 6–8 (W5-03) | 1 (`recipes.json`: бинт) |
| Предметы | каталог MVP | 1 (`items.json`: полевой бинт) |
| Улучшения | 3 уровня узлов | 1 (`base-upgrades.json`: склад L2) |
| Сетка инвентаря | 6×8, drag-and-drop | весовой лимит без сетки и drag-and-drop |
| Штраф смерти | по решению D-01 | **отсутствует** (см. ниже) |

Всё, что подтверждено автотестами, проверено на уровне чистых функций в `code/src/domain/domain.test.ts`; браузерной проверки UI базы не было.

## Фактический M2 scope

В `code/` реализован короткий hub перед и после одной миссии:

- versioned JSON-каталоги для активной миссии, награды, рецепта, предмета и улучшения;
- отдельные `stash` и `backpack`: stash без лимита, backpack имеет весовой лимит;
- перенос ресурсов и предметов между пулами; при возвращении backpack разгружается в stash;
- один рецепт бинта, атомарно списывающий ткань из stash;
- одно data-driven улучшение склада, увеличивающее допустимый вес backpack;
- медотсек тратит бинт из stash и лечит только HP;
- ремонт тратит металл из stash и восстанавливает только durability persistent equipment instance.

Нет real-time таймеров, оплаты или рекламы в core loop.

## M2 data and invariants

| Сущность | Источник | Правило |
|---|---|---|
| Миссия | `code/public/config/missions.json` | активная миссия должна ссылаться на существующие arena/reward/zone |
| Награда | `code/public/config/rewards.json` | складывается в stash, не теряется из-за веса backpack |
| Рецепт | `code/public/config/recipes.json` | preflight ресурсов → списание → output; нет частичной транзакции |
| Улучшение | `code/public/config/base-upgrades.json` | применяется только на следующий уровень узла |
| Экипировка | `SaveData.inventory.equipment` | instance id/slot уникальны, `0 ≤ durability ≤ maxDurability` |

## Acceptance criteria M2

Первые три подтверждены автотестами в `code/src/domain/domain.test.ts`:

- Возвращение разгружает backpack в stash без удаления предметов.
- Крафт не меняет inventory при отсутствии ресурсов.
- Repair не меняет HP, medbay не меняет durability.

Четвёртый критерий **не проверен**: «все действия доступны кнопками touch/keyboard-compatible UI» — это статическое чтение разметки и CSS, а не браузерная проверка. Ни одного прогона на устройстве не было; в репозитории нет headless-браузера и DOM-тестов. Локальное сохранение действий подтверждено тестами `save.test.ts`.

## Открытые решения / не реализовано

- Полная сетка 6×8, drag-and-drop, planner presets и весь каталог узлов/рецептов.
- Полный набор слотов экипировки и боевое расходование quick slots.
- Экономический баланс и несколько зон.
- **Штраф смерти отсутствует в коде.** Поражение переводит encounter в `failed`, допускает retry и **не отнимает ни XP, ни ресурсы, ни предметы**; `firstDeathReturnUsed` только помечает первый возврат и ни на что больше не влияет. Единая формула штрафа — решение D-01, реализация — `W4-02` и `W5-05`. До этого «смерть имеет цену» является дизайн-намерением, а не поведением игры.

## Post-M2

Расширять catalog-driven данные и UI только после UX-теста переноса stash/backpack на 360×640 и 390×844.
