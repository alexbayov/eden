{
  "name": "operation-isometric-slice",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "analyze:budget": "node scripts/check-bundle-budget.mjs",
    "preview": "vite preview",
    "lint": "eslint .",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "test:e2e": "npm run build && playwright test",
    "test:e2e:headed": "npm run build && playwright test --headed",
    "simulate:balance": "tsx src/sim/cli.ts --scope balance",
    "simulate:economy": "tsx src/sim/cli.ts --scope economy --mode chain",
    "verify": "npm run lint && npm run typecheck && npm run test && npm run build && npm run analyze:budget"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "phaser": "^4.2.1",
    "preact": "^10.29.7"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "1.62.1",
    "@preact/preset-vite": "^2.10.6",
    "@testing-library/preact": "3.2.4",
    "@types/node": "^24.13.3",
    "eslint": "^10.8.1",
    "eslint-plugin-preact": "^0.1.0",
    "globals": "^17.3.0",
    "jsdom": "30.0.1",
    "tsx": "4.23.12",
    "typescript": "~6.0.2",
    "typescript-eslint": "^8.67.0",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  }
}
